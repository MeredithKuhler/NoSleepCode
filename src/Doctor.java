public class Doctor extends Account {
    public Doctor(String lastName, String firstName, String password, int ID, String userName) {
        super(lastName, firstName, password, ID, userName);
    }
}
